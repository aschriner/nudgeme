# NudgeMe

A tiny tool to remind me to do stuff if I haven't done it.

### Usage
Run `python app/watcher.py` on a schedule.

### Tests
`$ pytest tests/functional_tests.py`

Not sure why pytest is not auto-discovering that test file though.  TODO: investigate.
